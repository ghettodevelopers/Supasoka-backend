# Namecheap Production Environment Variables
# This is a template for deploying Supasoka backend on Namecheap shared/VPS hosting
# Copy this file to .env and update with your actual values

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# PostgreSQL/MySQL connection string from your Namecheap hosting
# Format for PostgreSQL: postgresql://username:password@hostname:port/database_name
# Format for MySQL: mysql://username:password@hostname:port/database_name
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# ==========================================
# SERVER CONFIGURATION
# ==========================================
# Port number - Namecheap may assign a specific port or use 10000
PORT=10000

# Node environment - always 'production' for Namecheap deployment
NODE_ENV=production

# Host binding - use 0.0.0.0 to accept connections from all interfaces
HOST=0.0.0.0

# ==========================================
# JWT CONFIGURATION
# ==========================================
# IMPORTANT: Generate a strong, unique secret for production
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secure_jwt_secret_key_here_replace_this

# JWT token expiration time (7 days recommended)
JWT_EXPIRES_IN=7d

# ==========================================
# ADMIN CREDENTIALS
# ==========================================
# Default admin account - CHANGE THESE IN PRODUCTION!
ADMIN_EMAIL=admin@supasoka.com
ADMIN_PASSWORD=admin123

# ==========================================
# CORS CONFIGURATION
# ==========================================
# Allowed origins for CORS - UPDATE WITH YOUR ACTUAL NAMECHEAP DOMAINS
# Separate multiple origins with commas (no spaces)
# Include your frontend domain, API domain, and mobile app origins
ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com,https://www.yourdomain.com,capacitor://localhost,ionic://localhost,http://localhost

# ==========================================
# RATE LIMITING
# ==========================================
# Rate limit window in milliseconds (15 minutes = 900000ms)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window per IP
RATE_LIMIT_MAX_REQUESTS=100

# ==========================================
# NOTIFICATION CONFIGURATION (Optional)
# ==========================================
# Notification check interval in milliseconds (30 seconds = 30000ms)
NOTIFICATION_SCHEDULE_INTERVAL_MS=30000

# ==========================================
# PUSH NOTIFICATIONS (Optional - if using Pushy.me)
# ==========================================
# Get your API key from https://pushy.me
# PUSHY_SECRET_API_KEY=your_pushy_api_key_here

# ==========================================
# CLOUDINARY (Optional - for image uploads)
# ==========================================
# Get credentials from https://cloudinary.com
# CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
# CLOUDINARY_API_KEY=your_cloudinary_api_key
# CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ==========================================
# FRONTEND URLS (for reference)
# ==========================================
# These are used by your frontend apps to connect to the backend
# Update with your actual Namecheap domain
# REACT_APP_API_URL=https://api.yourdomain.com/api
# REACT_APP_SOCKET_URL=https://api.yourdomain.com

# ==========================================
# DEPLOYMENT NOTES
# ==========================================
# 1. Replace all 'yourdomain.com' with your actual Namecheap domain
# 2. Generate a strong JWT_SECRET using the command above
# 3. Update DATABASE_URL with your Namecheap database credentials
# 4. Change ADMIN_EMAIL and ADMIN_PASSWORD to secure values
# 5. Ensure your Namecheap hosting supports Node.js (version 18+)
# 6. Install dependencies: npm install
# 7. Run migrations: npm run migrate
# 8. Start server: npm start
